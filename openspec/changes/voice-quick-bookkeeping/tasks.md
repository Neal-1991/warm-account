## 1. 基础配置与服务端边界

- [ ] 1.1 确认测试环境和生产环境的 ASR 配置项、腾讯云密钥注入方式及最小权限策略
- [ ] 1.2 为语音能力建立独立的 `voice-entry` 云函数目录，补齐满足 CloudBase AI 调用要求的 `wx-server-sdk` 版本
- [ ] 1.3 为语音请求定义统一 requestId、itemId、阶段状态和脱敏错误码，并确定客户端可见错误文案
- [ ] 1.4 实现 `batchCreate` 的预校验 + 幂等标记 + 逐条写入 + 失败补偿删除策略，写出补偿未完成的孤儿记录日志和兜底规则

## 2. 小程序录音与入口

- [ ] 2.1 在首页新增语音记账入口，保留现有手工记账入口和原有导航
- [ ] 2.2 实现录音授权、开始、结束、取消、30 秒自动停止、重新录音和状态展示
- [ ] 2.3 将录音以受支持的格式上传到临时云存储，并限制文件时长、大小和并发请求
- [ ] 2.4 实现 ASR 处理中、解析处理中、失败、重试和手工记账回退状态，确保所有 loading/error 状态可结束
- [ ] 2.5 实现识别文本编辑页，确保继续解析使用用户修改后的文本

## 3. ASR 云函数

- [ ] 3.1 实现云函数鉴权、账本上下文校验和临时文件标识校验
- [ ] 3.2 在服务端生成腾讯云 ASR 请求签名，固定使用中文 16k 短语音参数并调用 SentenceRecognition
- [ ] 3.3 实现 ASR 超时、有限重试、鉴权/额度/参数错误分类和统一响应
- [ ] 3.4 在成功、失败、超时和异常路径清理临时音频，不向客户端返回临时文件 URL
- [ ] 3.5 增加 ASR 云函数单元测试或可重复的集成测试，覆盖空文本、中文数字、超长音频和服务错误

## 4. 规则解析与 Hy3 解析

- [ ] 4.1 实现金额解析规则，覆盖阿拉伯数字、中文数字、元/块/毛/角等常见表达并统一转换为整数分
- [ ] 4.2 实现收支类型、相对日期、备注和当前账本分类名称匹配规则
- [ ] 4.3 实现明确多笔表达的拆分规则，禁止对 AA、转账、借贷和复杂分摊进行静默推断
- [ ] 4.4 实现规则无法完整解析时的 Hy3 调用，使用当前非资源点环境适用的 `hunyuan-v3` provider 和 `hy3` 模型
- [ ] 4.5 为 Hy3 编写固定提示词、JSON schema、输出解析、字段校验和一次受限修复流程
- [ ] 4.6 将模型返回分类名称映射到当前账本真实 categoryId，未知分类标记 needsReview 而不是创建分类
- [ ] 4.7 增加解析测试集，覆盖单笔、多笔、中文数字、相对日期、错别字、未知分类、空金额和复杂金融语义

## 5. 草稿预览与批量写入

- [ ] 5.1 定义前端临时草稿数据结构，展示 transcript、items、warnings、confidence 和 needsReview
- [ ] 5.2 实现逐项编辑、删除和分类选择，复用现有手工记账字段校验与分类组件
- [ ] 5.3 实现确认前的完整客户端校验，并在服务端再次校验用户、账本、类型、金额、日期、分类和内容安全（复用现有 `assertRecordContentSafe`，覆盖备注和图片）
- [ ] 5.4 实现 requestId/itemId 幂等检查，防止重复点击、网络重试和页面恢复造成重复记录
- [ ] 5.5 实现预校验 + 幂等标记 + 逐条写入 + 失败补偿删除，明确异常时的成功、失败和待确认状态
- [ ] 5.6 复用现有记录云函数的权限和预算提醒逻辑，避免语音流程形成旁路写入口
- [ ] 5.7 确认并实现最小来源元数据（source、voiceRequestId、voiceItemId）的存储和查询策略；不得保存原始音频、音频 URL 或默认完整 transcript

## 6. 安全、成本和可观测性

- [ ] 6.1 将 ASR 和 AI 凭证移入云函数服务端环境配置，检查小程序包和仓库中不存在真实密钥
- [ ] 6.2 对单用户、单账本和全局增加录音时长、并发、频率和 AI/ASR 调用限制
- [ ] 6.3 实现阶段化日志和 requestId 关联，确保不记录密钥、完整音频 URL、完整 prompt 或不必要的完整文本
- [ ] 6.4 实现外部服务超时、额度不足和连续失败时的降级/熔断以及手工记账回退
- [ ] 6.5 将本地保存的密钥文件移出版本控制范围，增加精确的忽略规则；若密钥曾被误暴露则完成轮换

## 7. 测试、发布与回滚

- [ ] 7.1 在测试环境部署语音云函数，先验证 ASR，再验证 Hy3，再验证批量记录写入
- [ ] 7.2 使用真机验证录音授权、弱网、权限拒绝、重复点击、后台切换、超时和手工回退
- [ ] 7.3 验证现有登录、首页、手工记账、统计、记录详情和预算提醒不受影响
- [ ] 7.4 检查主包大小、图片/音频资源、废弃 API、云函数错误处理和跨用户文件访问规则
- [ ] 7.5 以灰度方式开放语音入口，观察 ASR 成功率、解析需要复核比例、重复写入率和单用户成本
- [ ] 7.6 准备回滚开关：隐藏语音入口并停止新语音请求；保留既有手工记账和已成功写入的合法记录
