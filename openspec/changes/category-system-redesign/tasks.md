## 1. 云函数 — category 扩展

- [x] 1.1 新增 `addBig` action：创建大类（parentId=null, bookId=当前账本, isSystem=false），自动创建"其他"子类
- [x] 1.2 新增 `rename` action：更新分类名称和 emoji（仅限 bookId=当前账本的分类）
- [x] 1.3 新增 `deleteChild` action：小类删除，无记录直接删，有记录时传入归并目标 categoryId，更新关联记录后删除
- [x] 1.4 新增 `deleteBig` action：仅 isSystem=false 可删，有子类级联删除，有关联记录则拒绝并返回记录数
- [x] 1.5 新增 `migrate` action：存量账本迁移（幂等），复制大类 → 更新子类 parentId → 更新记录 categoryId

## 2. 云函数 — init-database 重写

- [x] 2.1 更新预设分类定义：支出 16 大类按新顺序，收入 7 大类按新顺序
- [x] 2.2 改为 per-book 模式：所有分类 `bookId` 设为当前账本 ID，`isSystem: true`
- [x] 2.3 移除共享分类逻辑（不再创建 `bookId: null` 的分类）

## 3. 云函数 — record 扩展

- [x] 3.1 `list` 查询新增 `.orderBy('createdAt', 'desc')`（已在 fix/record-sort-order 分支实现，确认不遗漏）

## 4. 前端 — category-picker 组件改造

- [x] 4.1 右侧顶部新增"记在「XXX」下"选项，选大类时触发 select 事件返回大类信息
- [x] 4.2 左侧底部新增"+ 添加大类"入口，弹出输入框（名称 + emoji 选择）
- [x] 4.3 "管理"按钮改为跳转独立管理页 `wx.navigateTo({ url: '/pages/category-manage/category-manage' })`
- [x] 4.4 `onShow` 时重新加载分类（从管理页返回后刷新）

## 5. 前端 — 分类管理页（新增）

- [x] 5.1 创建页面文件：`pages/category-manage/category-manage.js|wxml|wxss|json`
- [x] 5.2 支出/收入 Tab 切换，分组展示大类和小类（大类带图标、名称、子类数、自建标签）
- [x] 5.3 大类操作：改名（含 emoji 选择）、添加大类、删除（仅自建，有子类时确认提示）
- [x] 5.4 小类操作：改名、删除（有记录时弹出归并目标选择）
- [x] 5.5 删除确认弹窗：根据关联记录数展示不同选项（归并到父类/同父其他子类/取消）
- [x] 5.6 注册页面到 `app.json`

## 6. 前端 — 记一笔页适配

- [x] 6.1 适配大类记账：`onCategorySelect` 处理 `isBigCategory` 标记，记录中使用大类 _id
- [x] 6.2 首页记录列表适配（现有 catInfoMap 已兼容大类记录）：大类记录展示大类图标+名称，小类记录展示父类图标+小类名称（同现网逻辑）

## 7. 整合测试

- [x] 7.1 微信开发者工具预览（已手工测试），验证：新增/改名/删除大类、新增/改名/删除小类、大类记账、小类记账
- [x] 7.2 验证存量数据迁移（已在测试环境手工验证）：测试环境调用 migrate，确认分类和记录正确
- [x] 7.3 验证首页排序（已在开发者工具中手工测试）：同日期按创建时间倒序 + 大类/小类均正确展示
- [x] 7.4 验证统计页（已在开发者工具中手工测试）：大类记账和小类记账均正确聚合到饼图
- [x] 7.5 检查主包大小 < 1.5M（~1.37M ✓），app.json lazyCodeLoading 已启用 ✓，app.json 中 `lazyCodeLoading` 已启用

## 8. 文档更新

- [x] 8.1 更新设计文档：3.3 分类模型、5.3 新增账目页、6.3 新增账目流程、8. 关键决策
- [x] 8.2 更新开发沟通记录：记录问题根因和修复方案
- [x] 8.3 更新问题追踪表：记录本次改造涉及的 issue
- [x] 8.4 更新测试用例：新增分类管理页、大类记账、分类改名/删除等用例

## 9. 上线前冒烟回归修复（2026-06-10）

- [x] 9.1 已存在部分旧版 per-book 分类时，登录幂等补齐新版 16 个支出大类和 7 个收入大类
- [x] 9.2 category/list 分页读取全量分类，避免超过云数据库单次查询上限后被截断
- [x] 9.3 分类管理页在 JS 中维护当前 Tab 列表，修复支出/收入列表为空
- [x] 9.4 修复新增大类时点击 emoji 候选不生效
- [x] 9.5 补充自动化回归测试和四份项目文档

## 10. 登录态、家庭加入与邀请过期修复（2026-06-12）

- [x] 10.1 新增 `login.restoreSession` 与 `app.ensureSession`，统一页面会话访问并支持主动退出标记
- [x] 10.2 邀请码生成返回过期时间并检查现存码碰撞，分享端不复用过期码
- [x] 10.3 首次加入在事务内重新校验邀请码并写入 `acceptedAt`，已接受任务允许过期续跑
- [x] 10.4 加入接口返回稳定错误码，前端严格核对目标 `bookId` 后才提示成功
- [x] 10.5 自动测试覆盖会话恢复、失效账本、主动退出、邀请过期和续跑边界
- [x] 10.6 同步 OpenSpec 与四份项目文档
- [x] 10.7 部署云函数并完成生产发布与 A/B 真机回归；受保护生产重置未作为常规动作执行，仅保留为明确需要时的受控工具
