# 暖账小程序 · 代码问题梳理报告

> 范围：对 `miniprogram/`（前端）、`cloudfunctions/`（云函数）、`openspec/`（设计规格）、`tests/`（测试）、`docs/`（文档）以及仓库根目录配置文件的一次完整只读审查。
> 方法：通读核心源码 + 跨文件检索 + 子代理专项审计（前端页面 / OpenSpec / 测试）。
> 说明：本报告**不修改任何代码**，仅记录问题、根因与修改建议。所有问题按「问题描述 / 问题原因 / 修改方案」三段式给出。

## 问题状态说明

- ✅ 已修复：代码已按修改方案落实
- 🚫 不处理：用户确认不修复（含风险接受/不在范围内）
- ⏳ 待修复：尚未处理，按建议优先级安排

---

## 一、严重程度总览

| # | 问题 | 位置 | 严重度 | 状态 |
|---|------|------|--------|------|
| 1 | 语音记账预算提醒文案永远显示硬编码兜底串（静默语义错误） | `voice-entry.js` + `voice-entry.wxml:172` | 🔴 严重 | ✅ 已修复 |
| 2 | `config.isTest = true` 被提交为默认，生产会读写测试集合 | `config.js:3` | 🔴 严重 | ✅ 已修复（isTest 移至 env.js） |
| 3 | 语音批量写入依赖的 `voiceRequests` 集合未初始化，且首查未做容错 | `record/index.js:410`、`init-collections`、`README` | 🔴 严重 | ✅ 已修复 |
| 4 | 所有云函数信任客户端 `event.isTest` 决定集合后缀 | 全部云函数 `getCollectionName` | 🟠 中等 | ⏳ 待修复 |
| 5 | `utils/cloud.js` 混用 ESM `export`，与全项目 CommonJS 冲突 | `miniprogram/utils/cloud.js` | 🟠 中等 | ⏳ 待修复 |
| 6 | `category` 批量迁移记录用 `limit` 不用 `skip`，靠副作用终止 | `category/index.js:497,653` | 🟠 中等 | ⏳ 待修复 |
| 7 | `voice-entry` 的 `recognize`/`parse` 未校验账本成员资格 | `voice-entry/index.js:36,120` | 🟠 中等 | ⏳ 待修复 |
| 8 | `category` 的 `migrate` 冗余且与 `init-database` 重复、可越权 | `category/index.js:587` | 🟠 中等 | ⏳ 待修复 |
| 9 | `category-emoji.js` 为死代码 | `miniprogram/utils/category-emoji.js` | 🟡 轻微 | ⏳ 待修复 |
| 10 | README 严重过时（页面/云函数/集合均缺项） | `README.md` | 🟡 轻微 | ⏳ 待修复 |
| 11 | `openspec` 任务清单未回填，规格与实现无法核对 | `openspec/.../tasks.md` | 🟡 轻微 | ⏳ 待修复 |
| 12 | 测试多为源码正则静态断言，未真正执行逻辑 | `tests/*.test.js` | 🟡 轻微 | ⏳ 待修复 |
| 13 | 月份范围时区不一致（list 本地 vs 预算 UTC） | `record/index.js:563`、`budget-utils.js:24` | 🟡 轻微 | ⏳ 待修复 |
| 14 | `clear-test-data` 未清理 `voiceRequests` | `clear-test-data/index.js:6` | 🟡 轻微 | ✅ 已修复（随问题3一并处理） |
| 15 | 明文云凭证存放在工作树（`暖账的API key.md`） | 仓库根 | 🟡 轻微 | 🚫 不处理 |
| 16 | 初始化文档分裂（runbook 与 init-collections 矛盾） | `docs/superpowers/plans/...runbook.md` | 🟡 轻微 | ⏳ 待修复 |
| 17 | 语音记账分类选择用 `wx.showActionSheet`，超过 6 项静默 fail | `voice-entry.js onItemCategoryTap` | 🟠 中等 | ✅ 已修复（上线前） |
| 18 | ASR 频率限制放在 `asr.js recognize` 函数内，内部重试被自己挡住 | `asr.js recognize` / `index.js recognize case` | 🔴 严重 | ✅ 已修复（上线前） |

> 备注：
> - 问题9 报告中"全仓无引用"不准确，`tests/category-system.test.js:37` 仍引用 `COMMON_EMOJIS` 等导出。删除时需同步清理测试用例。
> - 问题12 并非"测试多为静态正则断言"——`voice-quick-entry.test.js` 对 `local-parser` 有真实可执行测试；准确说法是"云函数与前端流程缺可执行测试，靠源码正则校验，无法捕获运行时语义错误"。

---

## 二、严重问题（🔴）

### 问题 1：语音记账成功后的预算提醒文案永远显示硬编码兜底串 ✅ 已修复

**问题描述**
语音记账批量写入成功后，成功页的预算提醒区域固定显示「该分类本月预算已接近上限」，无论真实状态是「接近上限 / 已用完 / 已超支 / 总预算」都显示同一句话，且标题丢失。

**问题原因**
- 后端 `record` 云函数返回的 `budgetAlert` 结构是 `{ level, scope, categoryName, usedAmount, limitAmount, remainingAmount, percent, ... }`，**没有 `title` 与 `message` 字段**（`cloudfunctions/record/budget-alert.js`）。
- 手工记账页 `add.js:490` 正确地调用了 `budgetAlertContent(res.result.budgetAlert)` 把后端结构格式化成 `{ title, message, ... }` 再渲染。
- 但语音记账页 `voice-entry.js` 的 `confirmBatch`（约 503 行）**直接把原始 `alert` 赋给 `budgetAlertView`**，未经过 `budgetAlertContent` 格式化。
- 前端 `voice-entry.wxml:172` 读取 `budgetAlertView.message || '该分类本月预算已接近上限'`，因原始结构无 `message`，永远落到硬编码兜底串。

**修改方案**
1. 在 `voice-entry.js` 的 `confirmBatch` 中复用 `utils/budget.js` 的 `budgetAlertContent`（该文件已被 `add.js` 引用，直接 `require` 即可）：
   ```js
   const { budgetAlertContent } = require('../../utils/budget')
   // ...
   const alert = budgetAlerts.find(a => a) || null
   this.setData({
     showBudgetAlert: !!alert,
     budgetAlertView: alert ? budgetAlertContent(alert) : null
   })
   ```
2. `voice-entry.wxml` 同时渲染 `title` 与 `message`（参照 `add` 页的弹窗结构），去掉硬编码兜底文字或仅作为极端兜底。

**修复落实**
- `miniprogram/pages/voice-entry/voice-entry.js`：顶部引入 `budgetAlertContent`；`confirmBatch` 中 `budgetAlertView: alert ? budgetAlertContent(alert) : null`。
- `miniprogram/pages/voice-entry/voice-entry.wxml`：预算提醒区改为渲染 `{{budgetAlertView.title || '预算提醒'}}` 与 `{{budgetAlertView.message || '该分类本月预算已接近上限'}}`，硬编码串仅作极端兜底。

---

### 问题 2：`config.isTest = true` 被提交为默认值，生产会读写测试集合 ✅ 已修复

**问题描述**
`miniprogram/utils/config.js:3` 写死了 `const isTest = true`（带 `// TODO: 修改 isTest 为 false 切换到生产环境` 注释）。这意味着随包发布出去的版本会把所有数据写到 `_test` 后缀集合（`books_test`/`records_test`/…）。一旦上线时忘记改回 `false`，真实用户数据全部进入测试库，且与文档要求的 `books_prod` 等集合错位。

**问题原因**
- 环境切换依赖一个被提交进仓库的常量，且默认值是测试环境。
- 没有发布期校验，纯靠人工记得改 `false`，极易遗漏。

**修改方案**
1. **不要**把 `true` 作为默认值提交到主干。改为从构建/环境变量注入，例如通过 `miniprogram/utils/env.js` 增加 `isTest` 字段（该文件本就 gitignore，适合放环境相关配置），`config.js` 改为 `require('./env').isTest`。
2. 或在 CI/发布脚本里加一条断言：发布包中 `isTest` 必须为 `false`，否则阻断发布。
3. 至少在 `README` 的「发布清单」里用醒目方式列出此步骤。

**修复落实**（原 🚫 不处理，2026-08-16 改按方案 1 实施）
- `env.example.js` / 本地 `env.js` 增加 `isTest` 字段；`config.js` 改为 `envConfig.isTest === true`，字段缺失时默认生产环境。
- 环境切换不再产生已提交文件的脏 diff，README「环境切换」说明同步更新。

---

### 问题 3：语音批量写入依赖的 `voiceRequests` 集合未被初始化，且首查未做容错 ✅ 已修复

**问题描述**
语音快速记账的批量写入走 `record` 云函数的 `batchCreate`，它用 `voiceRequests` 集合做幂等（防止重复写入）。但该集合：
- **未**出现在 `init-collections/index.js` 的 `COLLECTIONS` 列表；
- **未**出现在 `README.md` 的「初始化数据库」章节；
- `clear-test-data` 也不清理它（见问题 14）。

更关键的是，`batchCreate` 的**第一次**幂等查询（判断 `status === 'completed'`）位于外层 `try { switch ... }` 内、不在任何 `try/catch` 子块中（`record/index.js:410`）。如果该集合不存在，这次 `.get()` 会抛错，被最外层 `catch` 捕获，直接返回 `{ success: false, error: "collection voiceRequests_test does not exist" }`——**整个语音批量记账功能在集合未创建时完全不可用**。

> 注：`docs/superpowers/plans/2026-08-01-v3.4.0-voice-quick-bookkeeping-runbook.md` 里确实要求创建 `voiceRequests_prod`，说明它被当作「一次性手动步骤」记在某份发布 runbook 中，但标准初始化路径（init-collections / README）完全缺失它，极易在按文档部署时漏掉。

**问题原因**
- 语音功能是后期新增，初始化清单与文档未同步更新。
- `batchCreate` 对集合缺失的容错不一致：首查无容错，而后续的 `mark processing` / `mark completed` 都包了 `try/catch (non-fatal)`。

**修改方案**
1. 把 `voiceRequests` 加入 `cloudfunctions/init-collections/index.js` 的 `COLLECTIONS`，并在 `instructions` 与 `README` 的建集合清单中补上 `voiceRequests_test` / `voiceRequests_prod`，同时提示为 `(openId, requestId)` 建唯一索引。
2. 在 `clear-test-data/index.js:6` 的 `COLLECTIONS` 增加 `'voiceRequests'`，保证重置时一并清理。
3. 为健壮性，将 `record/index.js:410` 的幂等首查也用 `try/catch` 包住（best-effort）：集合不存在时降级为「不做幂等、直接写入」并打告警日志，避免功能因集合缺失而整体失败（与已有的 non-fatal 标记逻辑保持一致）。

**修复落实**
- `cloudfunctions/init-collections/index.js`：`COLLECTIONS` 新增 `{ name: 'voiceRequests', description: '语音记账幂等请求集合' }`；`instructions` 第 5 步补 `voiceRequests_test, voiceRequests_prod`，新增第 7 步提示为 `voiceRequests_*` 集合创建 `openId + requestId` 唯一索引。
- `cloudfunctions/clear-test-data/index.js:6`：`COLLECTIONS` 增加 `'voiceRequests'`（问题14 一并修复）。
- `cloudfunctions/record/index.js:409-420`：首查用 `try/catch` 包裹，集合缺失时降级为 `existing = null`（不做幂等、直接写入），仅打 `console.warn` 告警，与后续 mark processing/completed 的 non-fatal 策略一致。
- 注：README 第 10 项的更新归入问题 10「README 过时」一并处理。

---

## 三、中等问题（🟠）

### 问题 17：语音记账分类选择用 `wx.showActionSheet`，超过 6 项静默 fail ✅ 已修复（上线前）

**问题描述**
语音记账预览页「需补充分类」时，原用 `wx.showActionSheet` 弹出分类列表。微信该 API 的 `itemList` 硬限制最多 6 项，账本分类超过 6 个时 `success` 回调不触发（静默 fail），用户表现为「分类点不开」。

**问题原因**
- `wx.showActionSheet` 的 `itemList` 上限为 6，超出无显式报错，仅 `fail` 回调带 `errMsg: "selectMenu:fail"`，但代码未处理 fail 分支。
- 普通记账页已用自定义组件 `category-picker`（底部弹出、带图标、无数量限制），语音记账未复用。

**修改方案**
- 改为复用普通记账的 `category-picker` 自定义组件，通过 `bindtap` 打开、`bind:select` 接收选择结果。

**修复落实**
- `miniprogram/pages/voice-entry/voice-entry.json`：注册 `category-picker` 组件
- `miniprogram/pages/voice-entry/voice-entry.wxml`：分类行改为 `bindtap="onItemCategoryTap"` + 底部加 `<category-picker>` 组件
- `miniprogram/pages/voice-entry/voice-entry.js`：新增 `onItemCategoryTap`/`onCategorySelect`/`closeCategoryPicker`，替换原 `onItemCategoryTap` 的 ActionSheet 逻辑

### 问题 18：ASR 频率限制放在 `asr.js recognize` 函数内，内部重试被自己挡住 ✅ 已修复（上线前） 🔴 严重

**问题描述**
真机测试发现录音超 30 秒或空录音后，提示「请求过于频繁，请稍后再试」。但用户只请求了一次。

**问题原因**
- `asr.js` 的 `recognize` 函数每次被调用时，`checkRateLimit` 会**先记录时间戳再发请求**。
- `index.js` 的 `recognize` case 有重试循环（最多 2 次），ASR 失败（如空音频/超时）后会重试 → 再次调 `recognize` → `checkRateLimit` 发现 10 秒内重复 → 返回 `RATE_LIMITED`。
- 频率限制本意防用户快速重复请求，但放错位置（在 `recognize` 函数内），导致云函数**内部重试自己挡自己**。

**修改方案**
- 将 `checkRateLimit` 从 `asr.js` 的 `recognize` 函数移到 `index.js` 的 `case 'recognize'` 入口（重试循环外），只检查一次。内部重试不再受频率限制影响，用户级重复请求仍被限制。

**修复落实**
- `cloudfunctions/voice-entry/asr.js`：`recognize` 函数移除 `checkRateLimit` 调用
- `cloudfunctions/voice-entry/index.js`：import 补 `checkRateLimit`；`case 'recognize'` 入口（下载音频前）调一次 `checkRateLimit`，不通过直接返回 `RATE_LIMITED`；重试不重试列表移除已不会出现的 `RATE_LIMITED`

---

### 问题 4：所有云函数信任客户端传入的 `event.isTest` 决定集合后缀

**问题描述**
每个云函数都用 `getCollectionName(event, name)`，当 `event.isTest` 存在时，`true→_test`、`false→_prod`，且**缺省时默认 `_test`**（`record/index.js:18`、`book/index.js:17`、`category`、`budget`、`login`、`voice-entry` 同款逻辑）。即集合选择完全由客户端决定。

**问题原因**
- 注释说明「由于多个环境可能共用同一个云环境 ID，这里通过事件传递 isTest 参数」。这是一种便捷做法，但把环境隔离的主动权交给了不可信的客户端。

**修改方案**
- 推荐改为**服务端决定**：由云函数所在云环境本身（或云函数环境变量 `CLOUD_ENV_TAG`）映射出 `_test`/`_prod` 后缀，前端不再传 `isTest`。
- 若必须保留前端参数，至少：① 缺省值应定为生产（`_prod`）而非测试，避免「忘记传参就写测试库」；② 服务端加白名单校验，拒绝非法/越权切换。

### 问题 5：`miniprogram/utils/cloud.js` 混用 ESM `export`

**问题描述**
`cloud.js` 使用 `export function initCloud() ...` 与 `export default { ... }`（ES Module 语法），而项目其余所有模块均为 CommonJS（`module.exports` / `require`）。若某处 `require('./utils/cloud')` 该文件，会抛出 `SyntaxError: Unexpected token 'export'`（小程序运行时不支持在业务 JS 中用 ESM `export`）。

**问题原因**
- 该文件是早期/重构遗留物。当前所有页面均直接调用 `wx.cloud.*`，无任何地方引用它（全局检索无任何 `require('./cloud')`/`utils/cloud` 引用），属死代码。
- 其 `callFunction` 封装也与线上实际调用方式不一致，存在误导性。

**修改方案**
- 直接删除 `miniprogram/utils/cloud.js`，避免维护者误用或误以为它是统一的数据访问层。

### 问题 6：`category` 批量迁移记录用 `limit` 不用 `skip`

**问题描述**
`category/index.js` 的 `deleteChild`（约 497 行）与 `deleteBig`（约 653 行）在把记录从一个分类迁移到另一个分类时，循环体为：
```js
for (let i = 0; i <= total; i += batchSize) {
  const batch = await db.collection(...).where({ bookId, categoryId }).limit(batchSize).get()
  ...
}
```
**没有使用 `.skip(i)`**。

**问题原因**
- 该循环之所以能终止，是因为每次 `update` 把记录的 `categoryId` 改成目标后，该记录不再匹配 `where({ categoryId })`，下一次 `.limit(batchSize)` 取到的是剩余未迁移记录。这是**依赖副作用**的隐式终止，脆弱且低效；一旦将来更新逻辑变化（不再改变 categoryId），就会变成死循环。

**修改方案**
- 改为标准分页：`for (let i = 0; i < total; i += batchSize) { const batch = await query.skip(i).limit(batchSize).get(); if (!batch.data.length) break; ... }`，与本项目其它 `getAll` 辅助函数保持一致。

### 问题 7：`voice-entry` 的 `recognize`/`parse` 未校验账本成员资格

**问题描述**
`voice-entry/index.js` 的 `recognize`（36 行）与 `parse`（120 行）只检查 `bookId` 存在，就调用 ASR / 解析，并读取该账本的分类列表（`getCategories`）。未校验调用者 `openId` 是否属于该 `bookId` 的成员。

**问题原因**
- 其它云函数（record/book/category/budget）都通过 `assertBookMember` 校验成员资格，但 `voice-entry` 漏了这一步。

**修改方案**
- 在 `recognize`/`parse` 开头参考 book/record 的写法，加载账本并校验 `book.ownerId === openId || book.memberIds.includes(openId)`，否则返回 `{ success: false, error: 'permission denied' }`。既防资源（ASR 额度）滥用，也避免跨账本读取分类信息。

### 问题 8：`category` 的 `migrate` 动作冗余且可越权

**问题描述**
`category/index.js:587` 的 `migrate` 会遍历调用者所有账本，把 `bookId: null` 的系统预设分类复制进每个账本；且使用 `const migrationOpenId = openId || event.openId`，允许用请求参数覆盖 `openId`。

**问题原因**
- 新账本的分类初始化已由 `init-database` 的 `ensureBookPresets` 在登录时幂等完成（且 `login` 云函数会调用它）。`migrate` 是早期遗留的重复路径，逻辑复杂且 `event.openId` 覆盖带来轻微越权风险。

**修改方案**
- 若确认 `ensureBookPresets` 已覆盖所有新/老账本，建议**删除 `migrate` 动作**；若确需保留作一次性回填，去掉 `event.openId` 覆盖（只允许用服务端 `OPENID`），并加注释说明其一次性用途。

---

## 四、轻微问题（🟡）

### 问题 9：`category-emoji.js` 为死代码
全仓无任何文件 `require` 它（emoji 选择已由 `category-icons.js` 的别名机制替代）。建议删除。

### 问题 10：README 严重过时
- 声称「8 个页面」，实际 11 个（缺 `budget`、`category-manage`、`voice-entry`）。
- `cloudfunctions` 列表缺 `budget`、`clear-test-data`、`voice-entry`。
- 未提及 `voiceRequests` 集合、预算相关初始化与索引。
建议按现状重写「功能 / 技术栈 / 项目结构 / 初始化数据库」章节。

### 问题 11：OpenSpec 任务清单未回填
`openspec/changes/voice-quick-bookkeeping/tasks.md` 等提案的任务项全部为未勾选 `[ ]`，但对应功能已实现并通过源码测试。建议在实现完成后回填勾选状态，或在 `design.md` 标注「已实现」，否则「规格 vs 实现」无法核对。

### 问题 12：测试多为源码正则静态断言
`tests/voice-quick-entry.test.js`、`content-security.test.js` 等主要通过 `readSource` + `assert.match(code, /voiceRequests/)` 对源码做正则校验，并未真正 `require` 并执行页面逻辑或预算提醒格式化。因此问题 1 这类运行时语义错误不会被任何测试捕获。建议对 `budgetAlertContent`、语音 `confirmBatch` 的 alert 渲染、以及 `batchCreate` 幂等/补偿逻辑补充可执行断言（mock `wx.cloud`）。

### 问题 13：月份范围时区不一致
- `record.list`（`record/index.js:563`）用 `new Date(\`${month}-01\`)`（**本地时区**）构建查询范围；
- `prepareBudgetAlert`（同文件）与 `budget.getMonth` 用 `budget-utils.js:24` 的 `monthRange`（**UTC**）。
记录 `date` 以 UTC 存储（由 `new Date("YYYY-MM-DD")` 得到 UTC 零点）。在月初/月末边界，列表分组与预算统计可能各取一套时区，导致跨月记录归属不一致。建议统一时区策略（例如全程按本地日期字符串 `YYYY-MM-DD` 比较，或全程 UTC），并在 `record.list` 与 `budget` 两处保持一致。

### 问题 14：`clear-test-data` 未清理 `voiceRequests` ✅ 已修复（随问题3一并处理）
`clear-test-data/index.js:6` 的 `COLLECTIONS` 只有 books/records/categories/members/budgets，重置测试数据时 `voiceRequests` 中的幂等标记会残留。建议补上。

**修复落实**：`cloudfunctions/clear-test-data/index.js:6` 的 `COLLECTIONS` 末尾追加 `'voiceRequests'`。

### 问题 15：明文云凭证存放于工作树 🚫 不处理
仓库根 `暖账的API key.md` 以明文保存真实腾讯云 SecretId / SecretKey 与 CloudBase API Key（JWT）。
- **好的一面**：`.gitignore` 已排除 `暖账的API key.md` 与 `miniprogram/utils/env.js`，因此这些凭证**未进入版本库**；且 `asr.js` 实际从 `process.env.TENCENT_SECRET_ID/KEY` 读取（正确做法），源码中无硬编码密钥。
- **风险**：明文密钥留在项目目录仍有泄露隐患（整机备份、截图、误执行 `git add -A` 等）。建议删除该明文文件，凭证仅保留在云函数环境变量配置中；如必须留备忘，改为只记录「去哪配置、变量名是什么」，不写真实值。

### 问题 16：初始化文档分裂
`docs/superpowers/plans/2026-08-01-v3.4.0-voice-quick-bookkeeping-runbook.md` 要求创建 `voiceRequests_prod`，而 `init-collections` 与 `README` 未包含它（见问题 3）。说明「初始化步骤」分散在 runbook 与标准初始化之间。建议以 `init-collections` + `README` 为单一事实来源，runbook 仅引用，不再各写一套。

---

## 五、值得肯定的良好实践（供参考，非问题）

- **内容安全**：`record` 与 `category` 云函数对备注文本 / 图片做了 `msgSecCheck` / `imgSecCheck`，带超时与「不可用即拦截」的保守策略，符合小程序合规要求。
- **写操作原子性**：`book` 的 `removeMember` / `transferOwnership` 使用 `db.runTransaction`，降低并发风险。
- **幂等设计意图**：`batchCreate` 的 `voiceRequests` 幂等思路正确（completed → 直接返回、processing → 拒绝、失败 → 补偿删除），只是初始化与容错没配套（见问题 3）。
- **主题系统**：`theme.js` 用 CSS 变量 + `wx.setTabBarItem` 动态换肤，结构清晰，覆盖 9 套主题。
- **隐私合规**：`voice-entry` 在录音前走 `wx.requirePrivacyAuthorize` 与 `scope.record` 授权流程，`app.json` 也声明了隐私协议。
- **统一错误结构**：云函数普遍返回 `{ success, error, errorCode }`，便于前端分支处理。

---

## 六、建议修复优先级

1. **立即修复（发布前阻塞）**：问题 1（语音提醒语义错误）✅、问题 2（`isTest` 默认值）✅、问题 3（`voiceRequests` 集合初始化 + 首查容错）✅、问题 15（明文凭证）🚫。
   - 已完成：问题 1、2、3（含附带修复问题 14）。
   - 不处理：问题 15（用户确认）。
2. **本周内修复**：问题 4（环境选择服务端化）、问题 5（删 cloud.js）、问题 6（分页 skip）、问题 7（voice-entry 成员校验）、问题 8（删 migrate）。
3. **文档与测试债务**：问题 9–16，可在下一轮迭代中清理，其中问题 12（补可执行测试）对防止问题 1 类回归最有意义。

---

*生成方式：只读审查，未改动任何源代码。所有结论均基于仓库当前文件。*
