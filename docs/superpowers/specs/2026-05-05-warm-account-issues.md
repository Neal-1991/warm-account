# 暖账小程序 V1 问题记录

**版本：** V1.0
**日期：** 2026-06-04（更新：新增审核拒绝游客模式修复）
**状态：** 进行中

---

## 问题模板

```
| # | 优先级 | 标题 | 用例ID | 描述 | 复现步骤 | 期望结果 | 实际结果 | 截图 |

```

---

## 已记录问题

| # | 优先级 | 标题 | 用例ID | 描述 | 复现步骤 | 期望结果 | 实际结果 |
|---|---|---|---|---|---|---|---|
| 1 | 高 | cloud.getUserInfo is not a function | TC-001 | 点击登录时报错 | 点击"微信一键登录" | 正常登录 | cloud.getUserInfo 不存在 |
| 2 | 高 | 查询对象参数值不能均为undefined | TC-001 | 点击登录后弹框报错 | 点击"微信一键登录" | 正常登录 | context.OPENID 为 undefined |
| 3 | 中 | 我的页面没有显示微信头像和昵称 | TC-014 | 登录成功后查看我的页面 | 查看个人信息 | 显示头像和昵称 | 显示默认值"未登录" |

---

## 已修复问题

| # | 修复日期 | 问题描述 | 修复方案 |
|---|---|---|---|
| 1 | 2026-05-05 | cloud.getUserInfo is not a function | 使用 cloud.getWXContext().OPENID 替代不存在的 cloud.getUserInfo() |
| 2 | 2026-05-05 | 查询对象参数值不能均为undefined | 使用 cloud.getWXContext().OPENID 获取 openId |
| 3 | 2026-05-05 | 我的页面没有显示微信头像和昵称 | 添加用户信息持久化（app.js 添加 getUserInfo/setUserInfo 方法）+ 添加编辑功能 |
| 4 | 2026-05-05 | init-database 云函数缺少收入分类 | 将分类数据拆分为支出/收入，添加 type 字段，收入分类包括：工资、奖金、投资、兼职、闲置转卖 |
| 5 | 2026-05-06 | 分类选择器支出/收入未关联 | category云函数添加type过滤，add.js传递type，category-picker按type过滤大类和小类 |
| 6 | 2026-05-06 | 大类和小类没有关联 | init-database和login云函数中 `add()` 返回值 `id` 改为 `_id`，category-picker.wxml的 `wx:key` 和 `item.id` 改为 `item._id` |
| 7 | 2026-05-06 | 记一笔提交缺少categoryId | add.js 使用 `selectedCategory.id` 但组件返回的是 `categoryId`，修正字段名 |
| 8 | 2026-05-06 | 云函数add()返回id而非_id | book、record、category云函数中 `add()` 返回值 `id` 改为 `_id` |
| 9 | 2026-05-06 | 统计页分类显示为MongoDB _id | statistics.js 使用硬编码categoryNames，改为调用category云函数实时获取分类名称映射 |
| 10 | 2026-05-06 | 统计页占比显示1%而非100% | 百分比计算公式缺少乘以100，修复公式 |
| 11 | 2026-05-06 | 统计页饼图不显示（初步排查） | chartData和legend数据正常但饼图空白，初步排查ec-canvas组件或echarts渲染问题，最终由 #15 修复 |
| 12 | 2026-05-07 | 登录页需要勾选协议 | 添加checkbox勾选框，登录按钮需勾选后才能点击 |
| 13 | 2026-05-07 | 微信登录无法获取昵称头像 | 微信接口更新，登录时不再自动返回昵称头像，改为在"我的"页面提供"获取微信头像"按钮 |
| 14 | 2026-05-08 | 登录页勾选框勾选后不显示勾选标记 | checkbox color="#FFFFFF" 使勾号不可见，改为 #FF6B00 |
| 15 | 2026-05-08 | 统计页饼图不显示 | echarts.min.js是浏览器版，需要Canvas 2D节点+addEventListener桩函数；canvas缺id属性；ec初始为null |
| 16 | 2026-05-08 | 我的页编辑弹窗交互优化 | 将独立操作改为统一编辑表单：弹窗内直接编辑昵称、选择头像来源、保存按钮统一提交 |
| 17 | 2026-05-08 | 昵称输入不支持微信昵称一键填入 | input type="text" 无法使用微信昵称填充，改为 type="nickname" |
| 18 | 2026-05-08 | 家庭邀请流程不可用 | 全面重构邀请流程：分享卡片替代手动复制邀请码；加入前弹窗确认；加入时迁移历史数据+删除旧账本；邀请码1h有效期+一次性使用；登录云函数支持成员查询（`_.or`）；family页处理未登录暂存/有效/过期/已使用/已是成员/已在其他家庭等全场景 |
| 19 | 2026-05-09 | 头像昵称登出后丢失 | login 云函数新增 members 集合持久化 + updateProfile action；login 页优先使用云端 userInfo；mine 页保存时同步云端 |
| 20 | 2026-05-09 | 分类重复显示且无小类 | init-database 每次调用重复创建系统大类；category-picker 大类按名称去重 + 小类跨同名 _id 匹配 |
| 21 | 2026-05-09 | clear-test-data 遗漏 members 集合 | COLLECTIONS 数组添加 members |
| 22 | 2026-05-09 | 家庭成员显示为"成员1/成员2"+默认头像 | login 云函数新增 getMembers action；family.js 改为批量查询 members 集合获取真实昵称头像 |
| 23 | 2026-05-09 | 加入家庭后 B 的小类未迁移 | book/join 新增智能合并：同名同父合并（remap categoryId），不同名迁移 bookId |
| 24 | 2026-05-09 | 统计页按小类展示碎片化 | 改为按大类（父分类）聚合，构建 categoryId→父分类名映射，按金额降序排列 |
| 25 | 2026-05-09 | 已是成员点击邀请链接提示错误 | validateInviteCode 不传 openId，无法判断"已用"是本人还是他人；返回 already_member reason |
| 26 | 2026-05-09 | 首页记录图标和分类都显示餐饮 | index.js loadData() 硬编码 icon='📝' categoryName='餐饮'；改为先查分类构建映射再渲染 |
| 27 | 2026-05-09 | 加入家庭后统计页出现未分类 | book/join 中 col=books 被误用于查询 categories，导致 B 的小类从未迁移；改为 catCol |
| 28 | 2026-05-25 | 图片存储为临时路径，家庭成员无法查看 | 新建记录详情页（pages/detail/）；add.js 上传图片到云存储，记录中存 cloud file ID；首页记录卡片显示 📷 标记并支持点击跳转详情页 |
| 29 | 2026-05-25 | clear-test-data 未清理云存储图片 | 清理 records 前先收集所有 cloud file ID 并调用 cloud.deleteFile 删除，支持分批（最多50个/次） |
| 30 | 2026-05-25 | join 响应超时导致前端加入失败、弹窗不消失、bookId 未更新 | 云函数重排序（先加入成员再删旧账本）；前端 catch 中关闭弹窗 + 重新查询确认状态 |
| 31 | 2026-05-25 | 家庭成员间图片不可见 | record 云函数新增 getFileUrl action，通过 cloud.getTempFileURL 生成临时可访问 URL |
| 32 | 2026-05-28 | 头像选择后无法持久保留 | mine.js 保存前上传临时路径到云存储；login 云函数返回头像时 cloud:// 路径转 temp URL |
| 33 | 2026-05-28 | 已是成员点击邀请链接提示邀请无效 | join 成功后不再清空 inviteCode，保留码值供 validateInviteCode 查找到账本 |
| 34 | 2026-05-28 | 昵称更新后账本名称未同步 | mine.js 传递 bookId；login 云函数 updateProfile 检测所有者后同步更新账本名 |
| 35 | 2026-06-04 | 审核拒绝：一访问小程序就要求登录 | 改为游客模式：入口改为首页，未登录可浏览 Tab 和空状态，操作时按需引导登录（app.json / index.js / statistics.js / mine.js / login.js 共 7 个文件） |
| 36 | 2026-06-04 | 退出登录后我的页仍显示头像和昵称 | logout() 清除 userInfo（globalData + Storage）；重新登录后云函数从 members 集合恢复 |
| 37 | 2026-06-07 | 分类选择器切换大类后「记在XX下」名称为空 | category-picker 新增 selectedBigName 字段，initCategories/selectBig 时动态更新 |
| 38 | 2026-06-07 | 分类选择器滚动穿透到下层页面 | picker-mask 添加 catchtouchmove="preventTouchMove" |
| 39 | 2026-06-07 | 清空测试数据后预设分类只显示 4 个 | login 云函数新增 ensureCategories：已有账本自动 migrate/init-database，新账本自动创建预设分类，前端移除手动 init-database 调用 |
| 40 | 2026-06-07 | init-database 新增 updateSystemPresets action | 增量更新 bookId=null 系统预设（同名原地更新保留 _id，新大类追加），生产部署前执行确保 migrate 复制正确分类 |
| 41 | 2026-06-07 | 分类选择器内容无法滚动到确定按钮 | picker-content 改为 scroll-view 组件 + catchtouchmove="preventTouchMove" 仅遮罩层阻止穿透 |
| 42 | 2026-06-07 | 管理页修改分类后记账页不刷新 | add.js 新增 onShow() 重新加载分类，picker 组件 observer 自动刷新 |
| 43 | 2026-06-07 | book/join 分类合并按 parentId 匹配失败 | per-book 模型下 A/B 大类 _id 不同，改为按大类名称+小类名称匹配；新增大类记账记录重映射、parentId 重映射、自定义大类迁移 |
| 44 | 2026-06-09 | 云函数信任前端 openId/bookId 导致越权风险 | record/book/category/login.getMembers 统一使用 cloud.getWXContext().OPENID 做身份来源，并按账本成员/所有者校验访问权限 |
| 45 | 2026-06-09 | 删除大类未检查子类记录导致孤儿分类引用 | category/deleteBig 删除前统计大类本身和所有子类记录，存在记录时阻止删除并返回 recordCount |
| 46 | 2026-06-09 | 删除小类预检查错用 record/list 导致归并弹窗不可用 | record 云函数新增 countByCategory；category-manage 删除小类前改用精确计数接口 |
| 47 | 2026-06-09 | 记一笔切换收入/支出保留旧分类 | add.js 切换类型时清空 selectedCategory、关闭 picker、禁用提交，强制重新选择当前类型分类 |
| 48 | 2026-06-09 | 登录页使用已弃用 getUserInfo 授权入口 | login.wxml 移除 open-type="getUserInfo" / bindgetuserinfo；login.js 改为普通点击登录并使用云端资料或默认资料 |
| 49 | 2026-06-09 | clear-test-data 未清理成员头像云存储文件 | 清理云存储阶段同时收集 records.images 和 members.avatarUrl 中的 cloud file ID，去重后分批删除 |
| 50 | 2026-06-10 | 重新登录后支出分类仍只有旧版 6 类 | 原逻辑存在任意大类即跳过；改为 `categorySchemaVersion` + `presetKey` 幂等补齐，完整成功后才推进版本 |
| 51 | 2026-06-10 | 分类管理页支出/收入列表为空 | WXML 同节点 `wx:for` + 依赖 `item` 的 `wx:if` 作用域错误；改由 JS 维护 `visibleBigCategories` |
| 52 | 2026-06-10 | 新增大类时点击 emoji 候选不生效 | 候选点击与输入框不再共用处理函数：候选通过 `catchtap` + 数字 `data-index` 从固定列表取值，输入框使用 `e.detail.value`；两处新增入口共用候选列表，并放宽复合 emoji 长度 |
| 53 | 2026-06-10 | 系统分类改名后家庭合并可能匹配失败 | 系统分类新增稳定 `presetKey`，book/join 优先按稳定标识合并，旧数据按名称兜底 |
| 54 | 2026-06-11 | 首次点击加入家庭静默失败，第二次才成功 | join 串行执行且边迁移边删除，超时后留下半完成状态；前端错误地把 B 的个人账本当成目标账本。改为可续跑状态机，并仅按目标 bookId 判定成功 |
| 55 | 2026-06-11 | 加入家庭后大量记录显示未分类或小类无大类图标 | join 未分页读取全部分类且中断后丢失映射上下文。改为分页生成固定映射计划、迁移后完整性校验、最后清理重复分类 |
| 56 | 2026-06-11 | 春节红包记录不显示红包礼金图标 | 首页自行构建映射且父类缺失时退化为小类空图标。抽取统一分类映射，小类使用父大类图标和自身名称 |
| 57 | 2026-06-12 | 冷启动再次显示未登录但仍保留头像 | 页面只看内存状态且本地字段可能部分缺失；新增只读 `restoreSession`、统一 `ensureSession` 和主动退出标记 |
| 58 | 2026-06-12 | 家庭加入超时后误报成功并使用已删除旧账本 | 异常补偿把 B 的个人账本误当目标账本；改为可续跑任务并严格核对验证阶段目标 `bookId` |
| 59 | 2026-06-12 | 超过一小时的邀请仍可进入加入流程 | 分享页可能复用页面内旧码，首次接受缺少事务内最终校验；增加 `expiresAt`、服务端原子校验和稳定错误码 |
| 62 | 2026-06-15 | 用户无法为家庭账本设置月度消费目标 | 新增月度总预算、分类预算、复制上月、删除和成员只读查看能力 |
| 63 | 2026-06-15 | 首页和统计页无法表达预算使用情况 | 首页新增未设置/进行中/预警/超支卡片；统计页保留原图表并叠加总预算与分类预算进度 |
| 64 | 2026-06-15 | 预算与记账、分类删除、家庭合并缺少生命周期约束 | 记账后按首次跨线返回提醒；删除被当前/未来预算引用的大类时阻止；家庭合并保留目标预算并清理来源预算 |
| 65 | 2026-06-15 | 发布手册和集合检查未覆盖预算，隐私文案与实际行为不一致 | 重写发布审核手册；修正 `_test/_prod` 集合检测和预算映射；更新协议与隐私说明；移除文档中的管理员 OpenID |
| 66 | 2026-06-15 | 发布手册步骤过多，不适合当前项目规模 | 精简为 10 步正常发布流程，删除非常规生产重置和重复说明 |
| 67 | 2026-06-23 | 上月未设置预算时复制操作无可见反馈 | 增加 `PREVIOUS_BUDGET_NOT_FOUND` 错误码；前端先关闭 loading 再显示“上月未设置预算” |
| 68 | 2026-06-23 | 分类预算超支后继续记账不再提醒 | 分类达到或超过 100% 后每笔该分类支出都提醒；总预算仍只首次跨线 |
| 69 | 2026-06-23 | 预算提醒弹窗阻塞记账完成流程 | 改为提交按钮上方非阻塞提示条，约 2 秒后自动返回并刷新首页 |
| 70 | 2026-06-23 | 客户端版本号分散且旧实例不会主动重启更新 | 统一版本配置为 3.0.1，接入更新就绪强制重启和下载失败阻断 |
| 71 | 2026-06-25 | 冷启动首页记账记录约 10 秒后才显示 | 已修复：本地会话快速加载、远程会话后台校正、首屏加载去重、记录优先渲染、预算未设置快速返回；真机继续观察首屏耗时 |
| 72 | 2026-06-25 | 首页账目列表缺少按日期分组和分隔 | 已修复：前端生成 `recordGroups`，按日期显示分组标题和当日收入/支出小计，记录卡片在分组内隐藏重复日期 |
| 73 | 2026-07-02 | 已上线版本的任务状态和用户/发布材料滞后 | OpenSpec 手工验收项补记完成；用户手册、发布手册、memo、issues、testcases 和后续优化清单已同步到 `3.0.1` 生产状态 |

---

## 遗留问题

| # | 优先级 | 问题描述 | 原因 | 解决方案 |
|---|---|---|---|---|
| 1 | 低 | utils/cloud.js 封装的 callFunction 未被前端使用 | 各页面直接调用 wx.cloud.callFunction | 统一改用 cloud.callFunction 以复用错误处理 |
| 2 | 低 | isTest 硬编码于 config.js | const isTest = true，上线前需手动修改 | 改为环境感知（如读取云开发环境变量） |
| 3 | 低 | 记录列表无分页 | .limit(500) 硬限制，活跃账本可能丢失早期记录 | 添加基于游标的滚动分页 |
| 7 | 中 | 删除有记录小类时归并提示不准确 | 当前小类删除只支持归并到父大类或同父小类，提示文案容易让用户误以为可迁移到任意分类 | 下版优化删除/迁移交互，明确可选范围或新增跨分类迁移能力 |

---

## 已修复问题

| # | 修复日期 | 问题描述 | 修复方案 |
|---|---|---|---|
| ... | ... | ...（同上）| ... |
| 30 | 2026-06-07 | 冷启动登录态丢失 | app.js onLaunch 恢复 Storage→globalData；新增 getBookId/setBookId；login.js/family.js 统一使用 setBookId |
| 31 | 2026-06-07 | 同日期记录排序不稳定 | record/list 新增 .orderBy('createdAt', 'desc') |
| 32 | 2026-06-07 | 图片存储无环境区分 | add.js/mine.js 上传路径加 test/ 或 prod/ 前缀 |
| 33 | 2026-06-07 | 分类系统重构 | 每账本独立分类副本；大类/小类可增删改；支持大类记账；预设分类调整（支出16类/收入7类）；新增分类管理页；存量迁移 |
| 34 | 2026-06-10 | 分类初始化可能停留在旧版或半完成状态 | 新增分类结构版本和稳定预设标识；初始化中断不推进版本，下次登录幂等续补 |
| 35 | 2026-06-10 | 分类管理列表为空与新增 emoji 不生效 | 修正 WXML 循环过滤方式和 emoji 事件取值 |
| 36 | 2026-06-11 | 家庭加入不可重入且前端成功判定错误 | join 改为分阶段持久化迁移；前端自动续跑并严格核对目标 bookId |
| 37 | 2026-06-11 | 家庭合并后分类引用断裂 | 全量分页读取分类、固定映射、延迟删除、最终引用校验 |
| 38 | 2026-06-11 | 首页小类图标与名称口径不一致 | 统一为父大类图标 + 小类名；统计继续按父大类聚合 |
| 39 | 2026-06-12 | 冷启动部分登录态导致头像与登录状态矛盾 | 服务端只读恢复并校正 Storage；恢复失败时清除不完整资料；主动退出阻止静默登录 |
| 40 | 2026-06-12 | 家庭加入超时误判成功 | 前端只接受目标账本一致的 `completed`；服务端迁移状态持久化并支持续跑 |
| 41 | 2026-06-12 | 邀请过期边界不可靠 | 首次接受事务内复核服务端截止时间；写入 `acceptedAt` 后允许同用户跨过期时间续跑 |
| 60 | 2026-06-13 | 云函数控制台超时与代码配置不一致 | 记录 `config.json` 基线；发布时在控制台或 CLI 显式设置超时并复核，不能假设右键部署会应用 |
| 61 | 2026-06-13 | 发布手册要求在生产重复完整 A/B 与故障注入测试 | 完整业务回归移至 `_test`；`_prod` 仅做配置、集合、登录、单条记录增删和冷启动的最小冒烟 |
| 62 | 2026-06-15 | 用户无法为家庭账本设置月度消费目标 | 新增 `budgets` 集合、`budget` 云函数和预算管理页，支持总预算与分类预算 |
| 63 | 2026-06-15 | 首页和统计页无法表达预算使用情况 | 首页接入预算状态卡；统计页在不破坏原消费结构的前提下显示预算进度 |
| 64 | 2026-06-15 | 预算与相关数据生命周期不一致 | 增加跨线提醒、分类删除保护、家庭合并来源预算清理和测试数据清理 |
| 65 | 2026-06-15 | 发布流程、集合检查和隐私声明存在过期内容 | 发布手册升级为完整门禁流程；实际检查 10 个带后缀集合；补充预算配置并校正隐私文案 |
| 66 | 2026-06-15 | 发布手册过于复杂 | 保留配置、集合索引、云函数、测试、体验版和审核必需步骤，其余内容移除 |
| 67 | 2026-06-23 | 复制上月无预算时无反馈 | 服务端返回稳定错误码，前端调整 loading 与 toast 顺序 |
| 68 | 2026-06-23 | 家庭账本分类持续超支无提醒 | 服务端按账本总支出计算，分类持续超支每笔提醒且分类提醒优先 |
| 69 | 2026-06-23 | 记账预算提醒阻塞操作 | 移除确认弹窗，改为 2 秒非阻塞提示条并自动回首页 |
| 70 | 2026-06-23 | 版本不一致无法要求更新 | 新增共享版本配置和 `UpdateManager` 强制更新流程 |
| 73 | 2026-07-02 | 已上线版本状态和文档滞后 | 补记 OpenSpec 手工验收与生产发布状态，更新用户手册、发布手册、项目记录和后续优化清单 |

---

*后续新增功能、生产问题或发布收口继续更新本文件。*

---

## 2026-07-03 UI 色彩与图标问题补充

| # | 日期 | 问题描述 | 修复方案 |
|---|---|---|---|
| 74 | 2026-07-03 | 默认主题色观感不佳，白色主题下 tabBar、汇总卡和预算区域层次不清 | 建立统一主题 token，默认改为牛油果绿；白色主题改为白色界面 + 石墨灰强调色，汇总卡和预算卡增加边框、语义色和状态色 |
| 75 | 2026-07-03 | 主题只存本地会导致清缓存、换设备或重新登录后丢失 | 本地缓存按 openId 区分，并新增 `members.preferences.themeId` 云端同步；登录恢复时优先使用云端偏好 |
| 76 | 2026-07-03 | 分类 emoji 风格不统一，系统分类和自定义分类视觉割裂 | 新增 `iconKey` 与本地 PNG 图标系统，预设分类通过 `presetKey` 映射统一图标，旧 `icon` emoji 兜底 |
| 77 | 2026-07-03 | 统计页预算接口失败可能影响消费结构展示 | 统计页并行加载分类、记录、预算；预算失败仅降级预算信息，不阻塞饼图和分类占比 |
| 78 | 2026-07-03 | 家庭页暴露未完成的移除成员入口 | 隐藏移除成员按钮，保留后续正式功能空间 |
| 79 | 2026-07-03 | 详情页管理员删除按钮与后端权限不一致 | 详情页读取账本 owner，创建者或账本管理员显示删除按钮；云函数继续作为最终权限校验 |

## 2026-07-03 上线审查待跟踪项

| # | 优先级 | 问题描述 | 当前处理 | 后续建议 |
|---|---|---|---|---|
| 80 | 高 | 主题切换依赖 WXSS CSS 自定义属性和运行时 tabBar API，自动测试无法证明真机渲染 | 列为发布前手工门禁，开发者工具和真机必须验证所有主题关键区域同步变化 | 若发现页面主体未随主题变化，改为类名主题或显式样式兜底后再发布 |
| 81 | 中 | 主题云同步失败只记录日志，用户可能误以为已跨设备保存 | 本地优先生效，专项验收要求确认云端恢复 | 增加同步失败提示、重试队列或下次启动补偿 |
| 82 | 中 | 详情页管理员删除按钮在异常多账本或迁移态下可能被前端误隐藏 | 已改为 `record.get` 按 `recordId + bookId` 返回 `canModify`，详情页编辑/删除入口统一使用该权限 | 后端继续作为最终权限校验 |
| 83 | 低 | `themeId` 与 `iconKey` 主要由客户端规范化，云函数侧缺少白名单校验 | 未知值在客户端会回退默认主题或 emoji，不影响主流程 | 在 `login.updatePreferences` 和 `category.rename/addBig` 中增加白名单校验 |
| 84 | 高 | ignored 的 `miniprogram/utils/env.js` 在 worktree 中缺失，普通 git 状态和自动测试未暴露 | 已恢复本地 `env.js`，并在根目录 `AGENTS.md` 写入硬规则 | 每次 worktree 切换、预览、发布或修改 `config.js` 前，必须显式检查 `env.js` 存在且 `config.js` 可加载 |

## 2026-07-04 分类语义图标与主题弹层修复

| # | 日期 | 问题描述 | 修复方案 |
|---|---|---|---|
| 85 | 2026-07-04 | 预置 PNG、用户新增分类和历史 emoji 风格割裂，且带文字图标会与用户自定义分类名不一致 | 新增 34 个透明 PNG 语义图标，使用 `icon_*` key；新增 `ICON_ALIASES` 兼容旧 key；新增/改名大类入口改为按当前类型推荐并从语义图库选择，emoji 仅保留历史兜底 |
| 86 | 2026-07-04 | “我的 - 主题颜色”底部弹层在小屏上可能无法下滑看到全部主题 | 主题面板改为纵向 flex，标题固定，主题列表使用 `scroll-view scroll-y`，高度限制 `max-height: 78vh` 并预留底部安全区 |
| 87 | 2026-07-04 | 生产存量自定义大类仍只有历史 emoji，且“医保报销 💊”如果仅按 emoji 映射会误导为医疗支出 | 只读审计 `categories_prod` 后改为明确类型下先按分类名语义推荐，再使用 emoji alias 兜底；“医保报销”显示 `icon_receipt_return`，“汽车”显示 `icon_vehicle`，无需新增 PNG |
| 88 | 2026-07-04 | 账目输错后只能删除重记，带图片或跨月份记录修改成本高 | 记录详情新增编辑入口，复用记一笔页编辑态；新增 `record.get`，加固 `record.update` 权限和字段校验，编辑保存后刷新详情和首页缓存 |
| 89 | 2026-07-04 | 记一笔未选分类时显示默认 emoji，和统一 PNG 图标策略冲突 | 未选分类时不渲染 `category-icon`，只显示“请选择”；选择分类后再展示 PNG/emoji 兼容图标 |
| 90 | 2026-07-04 | 预算页大类预算行删除缺少确认且“删除”心智过重 | 行内操作改为“移除”，二次确认后只改预算草稿，并提示保存预算后生效 |
| 91 | 2026-07-04 | 首页切回时预算卡比记录慢且会闪成未设置状态 | 首页预算卡使用内存缓存和后台刷新；无缓存显示加载中，失败时不阻塞记录列表 |
| 92 | 2026-07-04 | 分类管理缺少账本级大类/小类排序能力 | 新增 `reorderBig` / `reorderChildren` 云函数和管理页上移/下移操作；小类仅允许同父排序，不支持跨大类迁移 |
| 93 | 2026-07-04 | 小程序头像与介绍表达需要更贴合当前家庭协作记账定位，但后台名称仅剩一次修改机会 | 保持小程序名称和应用内标题不变；生成 144x144 后台头像与 512x512 应用内 logo，登录页和关于页共用同一主视觉；介绍文案改为突出小家庭协作记账、预算、消费结构和邀请码共享账本 |
| 94 | 2026-07-05 | 腾讯审核打回【记一笔】内容安全机制不足 | `record.add/update` 写库前校验备注和图片，`category.addBig/addChild/rename` 写库前校验分类名称；违规只提示通用文案，接口异常时暂不保存，并清理本次新上传未入库图片 |
| 95 | 2026-07-05 | 首页自动刷新时偶现一闪而过的“数据加载失败”提示 | 已排查为首页某一次自动加载记录/分类请求失败时的统一 toast，可能由本地会话快速加载、后台会话校正或短暂网络抖动触发；当前用户感知影响较小，暂不处理，后续如需优化可改为“首次无数据页内失败态、已有数据时轻提示显示上次数据” |
